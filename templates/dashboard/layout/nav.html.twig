<nav class="h-screen p-4 top-bot-gradient rounded-tr-2xl rounded-br-2xl">
    <div class="flex justify-center">
        <a href="{{ path('app_dashboard_index') }}">
            <img src="{{ asset('img/logo/img.png') }}" alt="logo-img" class="w-14 h-14 bg-white p-1 rounded-lg">
        </a>
    </div>

    <p class="navigation-category">Modules</p>
    <ul class="main-navigation" class="mt-4">
        {% for module in activeModules %}
            <li><a href="{{ path(module.defaultRoute) }}"><i class="{{ module.icon }}"></i><span class="ps-3">{{ module.name }}</span></a></li>
        {% endfor %}
    </ul>

    <p class="navigation-category">Settings</p>
    <ul class="main-navigation" class="mt-4">
        <li><a href="#">Core</a></li>
        <li><a href="#">Modules</a></li>
    </ul>
</nav>
